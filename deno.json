{
  "importMap": "./deps.map.json",
  "tasks": {
    "bundle": "deno run --allow-all --no-check ./bundling.js",
    "run": "deno run --allow-all --no-check",
    "test": "deno test --allow-all --no-check  --parallel"
  }
}
